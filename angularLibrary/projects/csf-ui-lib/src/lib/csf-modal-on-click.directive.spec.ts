import { CsfModalOnClickDirective } from './csf-modal-on-click.directive';

describe('CsfModalOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CsfModalOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
