import { CsfInputDirective } from './csf-input.directive';

describe('CsfInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CsfInputDirective();
    expect(directive).toBeTruthy();
  });
});
